import Collapse from "./collapsable";
import { Toggle } from "./toggle";

export const SideBar = () => {
//fetch data
//fetch recommended lists and all

    return ( 
        <Collapse>
        {/* Any server component here */}
        <Toggle/>
        </Collapse>
    );
}
